# Week 1: Basic Math & Logic Foundations

This directory tracks my high-speed core DSA restart, focusing on raw mathematical optimizations and time complexity analysis.

### Solved Patterns:
* **Optimized Prime Check** — Implementation via square root boundary testing ($O(\sqrt{N})$) [INDEX].
* **Euclidean LCM & GCD** — Modulo-based division loop tracking to prevent integer overflow errors [INDEX].
* **Count Squares** — Structural boundary substitution using native logic ($i \times i < N$) to avoid system math functions [INDEX].
